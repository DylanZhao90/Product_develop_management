/**
 * PDM Design Tokens — re-exports from the preset system.
 *
 * For static access to the active theme tokens, use the ThemeProvider.
 * For direct preset access, import from "./presets".
 */

export { themePresets, presetMap, defaultPresetId } from "./presets";
export type { ThemePreset } from "./presets";
